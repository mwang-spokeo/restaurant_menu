%h1 Restaurant Menu
%span= link_to 'New Category', new_category_path, class: 'btn btn-sm btn-primary'

- @categories.each do |category|
  %div
    %h3
    = link_to category.title, category
    %ol
      - category.items.each do |item|
        %li= item.title
